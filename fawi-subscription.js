/*
 * Adaptador da subscrição mensal Fawi para o ZERO.
 *
 * NÃO colocar segredos/chaves privadas neste ficheiro.
 * O tutor deve ligar a chamada oficial da Fawi aqui.
 *
 * Exemplo do contrato esperado pelo ZERO:
 *
 * window.ZERO_FAWI_SUBSCRIBE = async ({ priceId }) => {
 *   // chamada oficial da Fawi
 *   // const result = await ...
 *   // return {
 *   //   success: true,
 *   //   subscriptionUntil: '2026-10-17T00:00:00.000Z'
 *   // };
 * };
 */
